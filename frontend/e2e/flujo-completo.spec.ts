import { test } from '@playwright/test';

/** Flujo extremo a extremo (registro → empresa → asiento → reportes). Pendiente hasta F6. */
test.fixme('registro → empresa → asiento → reportes', async () => {
  // Pendiente: se implementa en F6 cuando existan las pantallas y los navegadores instalados
});
