/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Pilot API
 * API REST de Pilot, ERP multiempresa para PYMES de El Salvador. Incluye el núcleo (usuarios y empresas) y la app Contabilidad. Los montos viajan como cadena decimal con 2 decimales (ADR-013) y los errores siguen RFC 9457 (Problem Details).
 * OpenAPI spec version: 1.0.0
 */
import type { AlcanceApiKey } from './alcanceApiKey';
import type { FechaHora } from './fechaHora';
import type { Uuid } from './uuid';

/**
 * API key de integración. Nunca incluye el secreto ni su hash.
 */
export interface ApiKey {
  id: Uuid;
  /** Nombre descriptivo, p. ej. "n8n producción". */
  nombre: string;
  /** Parte visible de la clave (pk_xxxx) para identificarla. */
  prefijo: string;
  /** Alcances que otorga la clave. */
  alcances: AlcanceApiKey[];
  /**
     * Instante UTC de expiración (FechaHora); nulo si no vence.
     * @nullable
     */
  expiraEn: string | null;
  /**
     * Instante UTC de revocación (FechaHora); nulo si está vigente.
     * @nullable
     */
  revocadaEn: string | null;
  /**
     * Instante UTC del último uso (FechaHora); nulo si nunca se usó.
     * @nullable
     */
  ultimoUsoEn: string | null;
  creadaEn: FechaHora;
}
