/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Pilot API
 * API REST de Pilot, ERP multiempresa para PYMES de El Salvador. Incluye el núcleo (usuarios y empresas) y la app Contabilidad. Los montos viajan como cadena decimal con 2 decimales (ADR-013) y los errores siguen RFC 9457 (Problem Details).
 * OpenAPI spec version: 1.0.0
 */
import type { ErrorCampo } from './errorCampo';
import type { MontoConSigno } from './montoConSigno';

/**
 * Error según RFC 9457 (application/problem+json) con extensiones de Pilot.
 */
export interface ProblemDetails {
  /** URI que identifica el tipo de problema; about:blank si no hay uno específico. */
  type: string;
  /** Resumen corto del problema, en español. */
  title: string;
  /** Código de estado HTTP de la respuesta. */
  status: number;
  /** Explicación del problema para esta ocurrencia; sin trazas ni datos internos. */
  detail?: string;
  /** URI o identificador de la ocurrencia concreta del problema. */
  instance?: string;
  /**
     * Código de negocio con prefijo del módulo (PLT plataforma, CON contabilidad, INT integración).
     * @pattern ^(PLT|CON|INT)-\d{3}$
     */
  codigo: string;
  /** Errores de validación por campo; solo en fallos de validación. */
  errores?: ErrorCampo[];
  /** Diferencia Σ Debe − Σ Haber; solo en descuadres y cobros que no cuadran. */
  diferencia?: MontoConSigno;
}
