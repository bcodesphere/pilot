/**
 * Contrato REST generado desde {@code api-spec/openapi/pilot-v1.yaml} (ADR-003): interfaces Spring y modelos.
 * Se expone como interfaz nombrada {@code contrato} para que los controladores de los demás módulos
 * puedan implementar las interfaces (un módulo la habilita con {@code allowedDependencies = "compartido::contrato"}).
 * El código de este paquete se genera en {@code target/generated-sources}; no se edita a mano.
 */
@NamedInterface("contrato")
package com.bcodesphere.pilot.compartido.api.contrato;

import org.springframework.modulith.NamedInterface;
