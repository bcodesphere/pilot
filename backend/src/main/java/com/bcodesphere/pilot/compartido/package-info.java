/**
 * Módulo compartido: tipos base (Dinero, EmpresaId, ModoPrecio), errores de dominio y utilidades.
 * No depende de ningún otro módulo (CLAUDE.md 4.2).
 */
@ApplicationModule(
        displayName = "compartido",
        allowedDependencies = {})
package com.bcodesphere.pilot.compartido;

import org.springframework.modulith.ApplicationModule;
